# -*- coding: utf-8 -*-
"""diabetes_prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1JS-yPNSMGgrqw8i14-EaW9wfY_tIS4AA

imoporting dependencies
"""

import pandas as pd
import numpy as np
from sklearn.preprocessing import StandardScaler
from sklearn.model_selection import train_test_split
from sklearn import svm
from sklearn.metrics import accuracy_score

"""Dataa collection and analysisss"""

dataset_diabetes = pd.read_csv('/content/diabetes.csv')

dataset_diabetes.head()

dataset_diabetes.shape

dataset_diabetes.describe()

dataset_diabetes['Outcome'].value_counts()

dataset_diabetes.groupby('Outcome').mean()

x = dataset_diabetes.drop(columns='Outcome',axis=1)
y=dataset_diabetes['Outcome']

print(x)

print(y)

"""data standarization"""

scaler = StandardScaler()

scaler.fit_transform(x)

standarazid_data= scaler.transform(x)

print(standarazid_data)

x =standarazid_data
y = dataset_diabetes['Outcome']

print(x)
print(y)

"""train test split"""

x_train ,x_test ,y_train ,y_test = train_test_split(x,y,test_size=0.2,stratify=y,random_state=2)

"""training thre modell"""

classifier = svm.SVC(kernel='linear')

classifier.fit(x_train,y_train)

"""model accuracy"""

x_train_predict = classifier.predict(x_train)
training_data_accuracy = accuracy_score(x_train_predict,y_train)

print('accuracy of the training data is : ', training_data_accuracy)

"""accuracy if test data
]
]

"""

x_test_prediction = classifier.predict(x_test)
testdata_accuracy = accuracy_score(x_test_prediction,y_test)
print('accuracy of test data is: ', testdata_accuracy)

"""making a predictivee systetm

"""

input_daata = (6,148,72,35,0,33.6,0.627,50)
#data -> nparrayy
input_data_np = np.asarray(input_daata)
#reshape array
input_data_reshape = input_data_np.reshape(1,-1)
 #standaraixze datwa
std_data = scaler.transform(input_data_reshape)
print(std_data)
prediction = classifier.predict(std_data)
print(prediction)
if(prediction[0]==0):
  print('the person is not diabetic')
else:
  print('the person is diabetic')

